@extends('layouts.app',['title'=>$category->name.' · DeshiBazaar Laos']) @section('content')<main class="mx-auto max-w-6xl px-4 py-10"><h1 class="text-3xl font-black">{{$category->name}}</h1><div class="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@foreach($products as $product)<a href="{{route('store.product',$product)}}" class="rounded-xl border bg-white p-5 shadow-sm"><h2 class="font-black">{{$product->title}}</h2><p class="mt-2 text-emerald-700">{{number_format($product->price,0)}} LAK / {{$product->unit}}</p></a>@endforeach</div></main>@endsection
