@extends('layouts.app',['title'=>$product->title.' · DeshiBazaar Laos','description'=>$product->description]) @section('content')<main class="mx-auto max-w-4xl px-4 py-10"><article class="rounded-xl bg-white p-6 shadow">@if($product->image)<img src="{{Storage::url($product->image)}}" alt="{{$product->title}}" class="mb-6 max-h-96 w-full rounded object-cover">@endif<h1 class="text-3xl font-black">{{$product->title}}</h1><p class="mt-3 text-slate-600">{{$product->description}}</p><p class="mt-5 text-2xl font-black text-emerald-700">{{number_format($product->price,0)}} LAK / {{$product->unit}}</p><form method="POST" action="{{route('cart.add',$product)}}" class="mt-6">@csrf<input name="quantity" type="number" min="1" value="1" class="w-20 rounded border-slate-300"><button class="ml-2 rounded bg-emerald-700 px-4 py-2 text-white">{{__('Add to cart')}}</button></form></article></main>@endsection
