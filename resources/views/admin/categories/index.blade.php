@extends('layouts.admin') @section('heading','Categories') @section('content')
<div class="grid gap-6 lg:grid-cols-[.8fr,1.2fr]"><form method="POST" action="{{route('admin.categories.store')}}" class="rounded-xl bg-white p-5 shadow">@csrf<h2 class="font-black">Add category</h2><input required name="name" placeholder="Category name" class="mt-4 w-full rounded border-slate-300"><textarea name="description" placeholder="Description" class="mt-3 w-full rounded border-slate-300"></textarea><label class="mt-3 block text-sm"><input type="checkbox" name="is_active" value="1" checked> Active</label><button class="mt-4 rounded bg-emerald-700 px-4 py-2 text-white">Create</button></form><div class="rounded-xl bg-white p-5 shadow"><h2 class="font-black">Manage categories</h2>@if(session('status'))<p class="mt-3 text-sm text-emerald-700">{{session('status')}}</p>@endif @foreach($categories as $category)<form method="POST" action="{{route('admin.categories.update',$category)}}" class="mt-4 border-t pt-4">@csrf @method('PUT')<div class="flex gap-2"><input name="name" value="{{$category->name}}" class="w-full rounded border-slate-300"><button class="rounded bg-slate-800 px-3 text-white">Save</button></div><div class="mt-2 text-xs text-slate-500">{{$category->products_count}} products · <label><input type="checkbox" name="is_active" value="1" @checked($category->is_active)> Active</label></div></form>@endforeach</div></div>@endsection
